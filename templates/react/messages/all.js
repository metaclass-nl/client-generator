import common_en from "./common-en";
//add import for each message file below

const all = {
  // add each ...importedconstant for en to the object below
  en: { ...common_en }
  // add objects with ...importedconstant for other languages below
}

export default all;